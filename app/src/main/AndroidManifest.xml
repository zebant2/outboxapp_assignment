<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.chinesetry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Practice" />
        <activity android:name=".Basics1" />
        <activity android:name=".Tipsnotes" />
        <activity android:name=".Basics2" />
        <activity android:name=".Blesson1" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".Blesson2" />
        <activity android:name=".Results"></activity>
    </application>

</manifest>